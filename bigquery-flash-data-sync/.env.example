# Example environment variables for the project.
# Copy this file to .env and replace placeholder values with real credentials.
# IMPORTANT: Do NOT commit your real .env to version control.

# Google Cloud  project identifier (e.g. "my_gcp_project")
GCP_PROJECT_ID=your_gcp_project_id

# BigQuery dataset where Salesforce and Finance tables will be loaded
BQ_DATASET_ID=your_bq_dataset

# Hostname or host:port of your DB server (e.g. 127.0.0.1 or db.example.com:5432)
DB_HOST=db.example.com:5432

# Finance database credentials
FINANCE_DB_NAME=finance_db
FINANCE_DB_USER=finance_user
FINANCE_DB_PASSWORD=password

# Salesforce database credentials
SALESFORCE_DB_NAME=salesforce_db
SALESFORCE_DB_USER=sales_user
SALESFORCE_DB_PASSWORD=password

# Maximum number of open connections to the DB (integer). Default: 20
DB_MAX_OPEN_CONNECTIONS=20

# Maximum number of idle connections to keep available (integer). Default: 10
DB_MAX_IDLE_CONNECTIONS=10

# Maximum duration for a single sync run. Use Go-style or common duration strings like "10m", "1h", "30s".
# Default if unset: 10m
SYNC_TIMEOUT=10m

# Maximum lifetime of a DB connection expressed as a duration 
DB_CONN_MAX_LIFETIME=1m

# Default date/time format used for normalizing values before loading to BigQuery.
# Use a clear format example. For ISO 8601 datetime, you might use: 2006-01-02T15:04:05Z07:00
# Or for date-only: 2006-01-02
DATE_FORMAT=2006-01-02T15:04:05Z07:00 # or DATE_FORMAT=YYYY-MM-DD

# Use 'dev' for development logging (more verbose), 'prod' for production
LOG_ENV=dev

# Log level: debug, info, warn, error, dpanic, panic, fatal
# Default: info (hides debug logs)
# Use 'debug' for detailed troubleshooting
LOG_LEVEL=info

# - Replace placeholders (your-gcp-project-id, your_bq_dataset, users, and passwords) before running.
# - For containerized or cloud deployments, set secrets via your platform's secret manager rather than committing them.
# - If your code expects DB_PORT separately or a full DATABASE_URL, add those variables here in the same pattern.
